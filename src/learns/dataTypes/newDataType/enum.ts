/**
 * Kieu enum la no gan trang thai cho mot phan tu
 * Khai bao enum trong ts: enum ten-bien{};
 */

enum workingStatus {
    "Free" = 10, "Working", "Accept-Order"
};

console.log(workingStatus.Free);
